diff -Nur multisync-0.82.orig/Makefile.am multisync-0.82/Makefile.am
--- multisync-0.82.orig/Makefile.am	2004-04-12 06:22:56.000000000 +0200
+++ multisync-0.82/Makefile.am	2004-06-03 20:46:36.000000000 +0200
@@ -12,7 +12,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(pkgdatadir)/pixmaps; \
+	  $(install_sh) -d $(DESTDIR)$(pkgdatadir)/pixmaps; \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(pkgdatadir)/pixmaps; \
@@ -20,7 +20,7 @@
 	  done \
 	fi
 	if test -d $(srcdir)/include; then \
-	  $(mkinstalldirs) $(DESTDIR)$(pkgincludedir); \
+	  $(install_sh) -d $(DESTDIR)$(pkgincludedir); \
 	  for includefile in $(srcdir)/include/*; do \
 	    if test -f $$includefile; then \
 	      $(INSTALL_DATA) $$includefile $(DESTDIR)$(pkgincludedir); \
@@ -28,7 +28,7 @@
 	  done \
 	fi
 	rm -f $(DESTDIR)$(pkgincludedir)/*~
-	$(mkinstalldirs) $(DESTDIR)$(desktopdir)
+	$(install_sh) -d $(DESTDIR)$(desktopdir)
 	$(INSTALL_DATA) $(desktop_file) $(DESTDIR)$(desktopdir)
 	@cat $(srcdir)/post-install
 
diff -Nur multisync-0.82.orig/plugins/backup_plugin/Makefile.am multisync-0.82/plugins/backup_plugin/Makefile.am
--- multisync-0.82.orig/plugins/backup_plugin/Makefile.am	2004-04-12 06:22:56.000000000 +0200
+++ multisync-0.82/plugins/backup_plugin/Makefile.am	2004-06-03 20:46:39.000000000 +0200
@@ -9,7 +9,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
diff -Nur multisync-0.82.orig/plugins/evolution_sync/Makefile.am multisync-0.82/plugins/evolution_sync/Makefile.am
--- multisync-0.82.orig/plugins/evolution_sync/Makefile.am	2004-04-12 06:22:57.000000000 +0200
+++ multisync-0.82/plugins/evolution_sync/Makefile.am	2004-06-03 20:46:42.000000000 +0200
@@ -2,14 +2,14 @@
 
 SUBDIRS = src 
 
-EXTRA_DIST = config.rpath mkinstalldirs  \
+EXTRA_DIST = config.rpath install_sh  \
 	autogen.sh \
 	evolution_sync.glade
 
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
diff -Nur multisync-0.82.orig/plugins/ldap_plugin/Makefile.am multisync-0.82/plugins/ldap_plugin/Makefile.am
--- multisync-0.82.orig/plugins/ldap_plugin/Makefile.am	2004-04-12 06:23:06.000000000 +0200
+++ multisync-0.82/plugins/ldap_plugin/Makefile.am	2004-06-03 20:46:46.000000000 +0200
@@ -9,7 +9,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
diff -Nur multisync-0.82.orig/plugins/opie_sync/Makefile.am multisync-0.82/plugins/opie_sync/Makefile.am
--- multisync-0.82.orig/plugins/opie_sync/Makefile.am	2004-04-12 06:23:06.000000000 +0200
+++ multisync-0.82/plugins/opie_sync/Makefile.am	2004-06-03 20:46:50.000000000 +0200
@@ -10,7 +10,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
diff -Nur multisync-0.82.orig/plugins/palm_sync/Makefile.am multisync-0.82/plugins/palm_sync/Makefile.am
--- multisync-0.82.orig/plugins/palm_sync/Makefile.am	2004-04-12 06:23:14.000000000 +0200
+++ multisync-0.82/plugins/palm_sync/Makefile.am	2004-06-03 20:47:07.000000000 +0200
@@ -9,7 +9,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
diff -Nur multisync-0.82.orig/plugins/palm_sync/conduit/Makefile.am multisync-0.82/plugins/palm_sync/conduit/Makefile.am
--- multisync-0.82.orig/plugins/palm_sync/conduit/Makefile.am	2004-04-12 06:23:13.000000000 +0200
+++ multisync-0.82/plugins/palm_sync/conduit/Makefile.am	2004-06-03 20:47:00.000000000 +0200
@@ -8,7 +8,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
diff -Nur multisync-0.82.orig/plugins/palm_sync/conduit/src/Makefile.am multisync-0.82/plugins/palm_sync/conduit/src/Makefile.am
--- multisync-0.82.orig/plugins/palm_sync/conduit/src/Makefile.am	2004-04-12 06:23:13.000000000 +0200
+++ multisync-0.82/plugins/palm_sync/conduit/src/Makefile.am	2004-06-03 20:47:03.000000000 +0200
@@ -27,4 +27,4 @@
 	m-address.conduit.in
 
 install-data-local:
-	$(mkinstalldirs) $(Conduitdir)
+	$(install_sh) -d $(Conduitdir)
diff -Nur multisync-0.82.orig/plugins/syncml_plugin/Makefile.am multisync-0.82/plugins/syncml_plugin/Makefile.am
--- multisync-0.82.orig/plugins/syncml_plugin/Makefile.am	2004-04-12 06:23:16.000000000 +0200
+++ multisync-0.82/plugins/syncml_plugin/Makefile.am	2004-06-03 20:47:11.000000000 +0200
@@ -9,7 +9,7 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d $(srcdir)/pixmaps; then \
-	  $(mkinstalldirs) $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
+	  $(install_sh) -d $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
 	  for pixmap in $(srcdir)/pixmaps/*; do \
 	    if test -f $$pixmap; then \
 	      $(INSTALL_DATA) $$pixmap $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE); \
